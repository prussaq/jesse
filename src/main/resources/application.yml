spring:
  datasource:
    url: jdbc:h2:mem:jessedb
  h2:
    console.enabled: true
  jpa:
#    defer-datasource-initialization: true // create tables from entities before
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  liquibase:
    change-log: classpath:changelog.yml
#  sql:
#    init:
#      mode: never

server:
  port: 9000
  servlet:
    context-path: /jesse

